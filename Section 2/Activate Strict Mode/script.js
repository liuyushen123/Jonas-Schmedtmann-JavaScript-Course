'use strict';

let hasDriverLicense = false;
const passTest = true;

if (passTest) {
    hasDriverLicense = true;
}

if (hasDriverLicense) {
    console.log('I can drive :D');
}

// const interface = 'Audio'
// const private = 534
// These variable name are not allowed due to strict mode and they are take to call a interface or make a variable private